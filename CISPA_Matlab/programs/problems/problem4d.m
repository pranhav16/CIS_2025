% --- TODO: Problem 4d ---
        % 1. Compute or load the frames F_A and F_D from the previous steps.
        %
        % 2. Read the `c_i` points from the calbody file.
        %
        % 3. Implement the logic to calculate C_expected. This will require
        %    functions for frame arithmetic (e.g., `invert_transform`, `apply_transform`).
        %    For each point `c_i`, calculate `C_expected_i = F_D * inv(F_A) * c_i`.
        %
        % --------------------------