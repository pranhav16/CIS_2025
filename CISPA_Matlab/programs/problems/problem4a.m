% --- TODO: Problem 4a ---
        % 1. Implement a function `read_cal_data.m` to parse the `d_i` points
        %    from 'pa1-debug-a-calbody.txt' and `A_i` points from 'pa1-debug-a-calreadings.txt'.
        %    This function should be robust enough to handle the file format.
        %
        % 2. Implement a function `point_cloud_registration.m`. This function should
        %    accept two sets of corresponding points and return the transformation
        %    (rotation matrix and translation vector) that aligns them, using Arun's method.
        %
        % 3. Call your functions here to compute the frame F_A.
        %
        % 4. Display and return the resulting transformation.
        % --------------------------
